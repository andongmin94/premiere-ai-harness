name: Product CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Product CI is paused only for the atomic Core 0.5.0 update commit."
